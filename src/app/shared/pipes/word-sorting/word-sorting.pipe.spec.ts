import { WordSortingPipe } from './word-sorting.pipe';

describe('WordSortingPipe', () => {
  it('create an instance', () => {
    const pipe = new WordSortingPipe();
    expect(pipe).toBeTruthy();
  });
});
